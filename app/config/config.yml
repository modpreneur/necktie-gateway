imports:
   - { resource: parameters.yml }

framework:
    secret: S0ME_SECRET

doctrine:
    dbal:
        driver:   %database_driver%
        host:     %database_host%
        dbname:   %database_name%
        path:     %database_path%
        user:     %database_user%
        password: %database_password%
    orm:
        auto_mapping: true

