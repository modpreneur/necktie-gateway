web_gateway:
  extends:
    service: web_gateway
  tty: true
  environment:
    - DEBUG=true
    - VIRTUAL_HOST=gateway.docker
  privileged: true
  hostname: gateway.docker
  domainname: gateway.docker
  privileged: true


